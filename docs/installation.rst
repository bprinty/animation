============
Installation
============

At the command line::

    $ pip install animation


Or, through GitHub::

    $ git clone http://github.com/bprinty/animation.git
    $ cd animation
    $ python setup.py install
